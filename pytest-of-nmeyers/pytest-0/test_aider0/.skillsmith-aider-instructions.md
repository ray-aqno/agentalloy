Before starting any task, POST to http://localhost:8000/compose/text with `{"task": "...", "phase": "build|spec|design|qa|ops"}`. The response is plain text skill context — read it before generating code or a plan.
