skill_type: domain
skill_id: webhooks-signature-verification
canonical_name: Webhooks — Signature Verification
category: design
skill_class: domain
domain_tags:
  - webhook-security
  - hmac-signing
  - svix-webhooks
  - timestamp-tolerance
  - replay-prevention
always_apply: false
phase_scope: null
category_scope: null
author: navistone
change_summary: machine authoring contract for pending-qa pipeline
raw_prose: |
  # Webhooks — Signature Verification

  ## Overview

  Webhooks must be authenticated to prevent forgery and replay attacks. The standard pattern: HMAC-sign the (timestamp + body) tuple, send the signature in a header, the consumer recomputes and compares constant-time. This skill covers verification end-to-end.

  Sourced from svix-docs receiving/verifying-payloads/why and how.

  ## Why Verify Webhooks

  Because of the way webhooks work, attackers can impersonate services by simply sending a fake webhook to an endpoint. Think about it: it's just an HTTP POST from an unknown source. This is a potential security hole for many applications, or at the very least, a source of problems.

  In order to prevent it, Svix signs every webhook and its metadata with a unique key for each endpoint. This signature can then be used to verify the webhook indeed comes from Svix, and only process it if it is.

  Another potential security hole is what's called replay attacks. A [replay attack](https://en.wikipedia.org/wiki/Replay_attack) is when an attacker intercepts a valid payload (including the signature), and re-transmits it to your endpoint. This payload will pass signature validation, and will therefore be acted upon.

  To mitigate this attack, Svix includes a timestamp for when the webhook attempt occurred. Our libraries automatically reject webhooks with a timestamp that are more than five minutes away (past or future) from the current time. This requires your server's clock to be synchronised and accurate, and it's recommended that you use [NTP](https://en.wikipedia.org/wiki/Network_Time_Protocol) to achieve this.

  For additional information about webhook vulnerabilities, please refer to the [webhook security](/security) section of the docs.

  ## How to Verify Webhooks with the Svix Libraries

  As shown in the [Why Verify section](./why.mdx), verifying operational webhooks is very important. This section describes how to do it.

  ### Verifying using our official libraries

  First install the libraries if you haven't already:

  <CodeTabs items={["JavaScript","Python","Rust","Go","Java","Kotlin","Ruby","C#","PHP","CLI","cURL"]}>

  <TabItem value="JavaScript">

  ```js
  npm install svix
  // Or
  yarn add svix
  ```

  </TabItem>
  <TabItem value="Python">

  ```sh
  pip install svix
  ```

  </TabItem>
  <TabItem value="Rust">

  ```toml
  http = "1.0.0"
  svix = "1.20.0"
  ```

  </TabItem>
  <TabItem value="Go">

  ```shell
  go get github.com/svix/svix-webhooks/go
  ```

  </TabItem>
  <TabItem value="Java">

  Gradle: Add this dependency to your project's build file:

  ```groovy
  implementation "com.svix:svix:0.x.y"
  ```

  Maven: Add this dependency to your project's POM:

  ```xml
  <dependency>
    <groupId>com.svix</groupId>
    <artifactId>svix</artifactId>
    <version>0.x.y</version>
  </dependency>
  ```

  </TabItem>
  <TabItem value="Kotlin">

  Gradle: Add this dependency to your project's build file:

  ```groovy
  implementation "com.svix.kotlin:svix-kotlin:0.x.y"
  ```

  Maven: Add this dependency to your project's POM:

  ```xml
  <dependency>
    <groupId>com.svix.kotlin</groupId>
    <artifactId>svix-kotlin</artifactId>
    <version>0.x.y</version>
  </dependency>
  ```

  </TabItem>
  <TabItem value="Ruby">

  ```shell
  gem install svix
  ```

  </TabItem>
  <TabItem value="C#">

  ```shell
  dotnet add package Svix
  ```

  </TabItem>
  <TabItem value="PHP">

  ```shell
  composer require svix/svix
  ```

  </TabItem>
  <TabItem value="CLI">

  On macOS install via <a href="https://brew.sh/">Homebrew</a>:
  ```sh
  brew install svix/svix/svix-cli
  ```

  On Windows install via <a href="https://scoop.sh/">Scoop</a>:
  ```sh
  scoop bucket add svix https://github.com/svix/scoop-svix.git
  scoop install svix
  ```

  For other platforms, such as linux, checkout the <a href="https://github.com/svix/svix-cli#installation">CLI docs</a> on Github.

  </TabItem>
  <TabItem value="cURL">

  ```shell
  # Install cURL. E.g. on arch linux:
  pacman -S curl
  ```

  </TabItem>
  </CodeTabs>

  Then verify webhooks using the code below. The payload is the raw (string) body of the request, and the headers are the headers passed in the request.

  <Callout type="error">

  **Use the raw request body**

  You need to use the raw request body when verifying webhooks, as the cryptographic signature is sensitive to even the slightest changes.
  You should watch out for frameworks that parse the request as JSON and then stringify it because this too will break the signature verification.

  See examples below for how to get the raw request body with different frameworks.

  </Callout>
  The signature you should get from where you added the endpoint, e.g. the application portal.

  <CodeTabs items={["JavaScript","Python","Rust","Go","Java","Kotlin","Ruby","C#","PHP","CLI","cURL"]}>

  <TabItem value="JavaScript">

  ```js

  const secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

  // These were all sent from the server
  const headers = {
    "svix-id": "msg_EXAMPLE_PLACEHOLDER",
    "svix-timestamp": "1614265330",
    "svix-signature": "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
  };
  const payload = '{"test": 2432232314}';

  const wh = new Webhook(secret);
  // Throws on error, returns the verified content on success
  wh.verify(payload, headers);
  ```

  </TabItem>
  <TabItem value="Python">

  ```python
  from svix.webhooks import Webhook

  secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET"

  # These were all sent from the server
  headers = {
    "svix-id": "msg_EXAMPLE_PLACEHOLDER",
    "svix-timestamp": "1614265330",
    "svix-signature": "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
  }
  payload = '{"test": 2432232314}'

  wh = Webhook(secret)
  # Throws on error, returns the verified content on success
  wh.verify(payload, headers)
  ```

  </TabItem>
  <TabItem value="Rust">

  ```rust
  use svix::webhooks::Webhook;

  let secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET".to_string();

  let mut headers = http::HeaderMap::new();
  headers.insert("svix-id", "msg_EXAMPLE_PLACEHOLDER");
  headers.insert("svix-timestamp", "1614265330");
  headers.insert(
      "svix-signature",
      "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
  );

  let payload = b"{\"test\": 2432232314}";

  let wh = Webhook::new(&secret)?;
  wh.verify(&payload, &headers)?;
  // returns Ok on success, Err otherwise
  ```

  </TabItem>
  <TabItem value="Go">

  ```go
  svix "github.com/svix/svix-webhooks/go"
  )

  secret := "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET"

  // These were all sent from the server
  headers := http.Header{}
  headers.Set("svix-id", "msg_EXAMPLE_PLACEHOLDER")
  headers.Set("svix-timestamp", "1614265330")
  headers.Set("svix-signature", "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=")

  payload := []byte(`{"test": 2432232314}`)

  wh, err := svix.NewWebhook(secret)
  err := wh.Verify(payload, headers)
  // returns nil on success, error otherwise
  ```

  </TabItem>
  <TabItem value="Java">

  ```java

  String secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

  // These were all sent from the server
  HashMap<String, List<String>> headerMap = new HashMap<String, List<String>>();
  headerMap.put("svix-id", Arrays.asList("msg_EXAMPLE_PLACEHOLDER"));
  headerMap.put("svix-timestamp", Arrays.asList("1614265330"));
  headerMap.put("svix-signature", Arrays.asList("v1,EXAMPLE_PLACEHOLDER_SIGNATURE="));
  HttpHeaders headers = HttpHeaders.of(headerMap, BiPredicate<String, String>)

  String payload = "{\"test\": 2432232314}";

  Webhook webhook = new Webhook(secret);

  webhook.verify(payload, headers)
  // throws WebhookVerificationError exception on failure.
  ```

  </TabItem>
  <TabItem value="Kotlin">

  ```kotlin

  val secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

  // These were all sent from the server
  val headersMap = mapOf(
      "svix-id" to listOf("msg_EXAMPLE_PLACEHOLDER"),
      "svix-timestamp" to listOf("1614265330"),
      "svix-signature" to listOf("v1,EXAMPLE_PLACEHOLDER_SIGNATURE="),
  )
  val headers = HttpHeaders(headersMap)

  val payload = "{\"test\": 2432232314}"

  val webhook = Webhook(secret)

  try {
      webhook.verify(payload, headers)
  } catch (e: WebhookVerificationError) {
      // Handle error
  }
  ```

  </TabItem>
  <TabItem value="Ruby">

  ```ruby
  require 'svix'

  secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET"

  # These were all sent from the server
  headers = {
    "svix-id" => "msg_EXAMPLE_PLACEHOLDER",
    "svix-timestamp" => "1614265330",
    "svix-signature" => "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
  }
  payload = '{"test": 2432232314}'

  wh = Svix::Webhook.new(secret)
  # Throws on error, returns the verified content on success
  wh.verify(payload, headers)
  ```

  </TabItem>
  <TabItem value="C#">

  ```csharp
  using Svix;

  string secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

  // These were all sent from the server
  var headers = new Dictionary<string, List<string>> {
      { "svix-id", new List<string> { "msg_EXAMPLE_PLACEHOLDER" } },
      { "svix-timestamp", new List<string> { "1614265330" } },
      { "svix-signature", new List<string> { "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=" } }
  };
  string payload = "{\"test\": 2432232314}";

  var webhook = new Webhook(secret);
  // Throws on error, returns the verified content on success
  webhook.Verify(payload, headers);
  ```

  </TabItem>
  <TabItem value="PHP">

  ```php
  use Svix\Webhook;

  $secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

  // These were all sent from the server
  $headers = [
      'svix-id' => ['msg_EXAMPLE_PLACEHOLDER'],
      'svix-timestamp' => ['1614265330'],
      'svix-signature' => ['v1,EXAMPLE_PLACEHOLDER_SIGNATURE=']
  ];
  $payload = '{"test": 2432232314}';

  $webhook = new Webhook($secret);
  // Throws on error, returns the verified content on success
  $webhook->verify($payload, $headers);
  ```

  </TabItem>
  <TabItem value="CLI">

  ```sh
  svix verify --secret whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET --body '{"test": 2432232314}' --headers 'svix-id=msg_EXAMPLE_PLACEHOLDER' 'svix-timestamp=1614265330' 'svix-signature=v1,EXAMPLE_PLACEHOLDER_SIGNATURE='
  ```

  </TabItem>
  <TabItem value="cURL">

  ```sh
  # Install cURL. E.g. on arch linux:
  pacman -S curl
  ```

  </TabItem>
  </CodeTabs>

  ## Verifying Webhooks Manually

  <Callout type="info">

  The recommended way to verify webhooks is using our official libraries as outlined in the [How to Verify](./how.mdx) section.

  </Callout>
  However, here are instructions for verifying manually for those who need it.

  ## Verifying signatures manually

  Each webhook call includes three headers with additional information that are used for verification:

  * `svix-id`: the unique message identifier for the webhook message. This identifier is unique across all messages, but will be the same when the same webhook is being resent (e.g. due to a previous failure).
  * `svix-timestamp`: timestamp in [seconds since epoch](https://en.wikipedia.org/wiki/Unix_time).
  * `svix-signature`: the [Base64](https://en.wikipedia.org/wiki/Base64) encoded list of signatures (space delimited).

  <Callout type="info">

  Professional and Enterprise tier customers can have the headers white-labeled to use the `webhook-` prefix instead of the `svix-` prefix used above. The Svix libraries support both.

  </Callout>
  ### Constructing the signed content

  The content to sign is composed by concatenating the id, timestamp and payload, separated by the full-stop character (`.`). In code, it will look something like:

  ```javascript
  const signedContent = `${svix_id}.${svix_timestamp}.${body}`;
  ```

  Where `body` is the raw body of the request. The signature is sensitive to any changes, so even a small change in the body will cause the signature to be completely different. This means that you should *not* change the body in any way before verifying.

  ### Determining the expected signature

  Svix uses an [HMAC](https://en.wikipedia.org/wiki/Hash-based_message_authentication_code) with [SHA-256](https://en.wikipedia.org/wiki/SHA-2) to sign its webhooks.

  So to calculate the expected signature, you should HMAC the `signed_content` from above using the base64 portion of your signing secret (this is the part after the `whsec_` prefix) as the key.
  For example, given the secret `whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET` you will want to use `MfKQ9r8GKYqrTwjUPD8ILPZIo2LaLaSw`.

  For example, this is how you can calculate the signature in Node.js:

  ```javascript
  const crypto = require('crypto');

  const signedContent = `${svix_id}.${svix_timestamp}.${body}`;
  const secret = "whsec_5WbX5kEWLlfzsGNjH64I8lOOqUB6e8FH";

  // Need to base64 decode the secret
  const secretBytes = Buffer.from(secret.split('_')[1], "base64");
  const signature = crypto
    .createHmac('sha256', secretBytes)
    .update(signedContent)
    .digest('base64');

  console.log(signature);
  ```

  This generated signature should match one of the ones sent in the `svix-signature` header.

  The `svix-signature` header is composed of a list of space delimited signatures and their corresponding version identifiers. The signature list is most commonly of length one. Though there could be any number of signatures. For example:

  ```
  v1,EXAMPLE_PLACEHOLDER_SIGNATURE= v1,bm9ldHUjKzFob2VudXRob2VodWUzMjRvdWVvdW9ldQo= v2,MzJsNDk4MzI0K2VvdSMjMTEjQEBAQDEyMzMzMzEyMwo=
  ```

  Make sure to remove the version prefix and delimiter (e.g. `v1,`) before verifying the signature.

  Please note that to compare the signatures it's recommended to use a constant-time string comparison method in order to prevent timing attacks.

  ### Verify timestamp

  As mentioned above, Svix also sends the timestamp of the attempt in the `svix-timestamp` header. You should compare this timestamp against your system timestamp and ensure that it falls within an acceptable tolerance (e.g., ±5 minutes). This helps prevent replay attacks by ensuring that the webhook is not too old or too new.

  If the timestamp is outside the acceptable range, you should reject the webhook to prevent unauthorized access.
fragments:
  - fragment_type: setup
    sequence: 1
    content: |
      Webhooks must be authenticated to prevent forgery and replay attacks. The standard pattern: HMAC-sign the (timestamp + body) tuple, send the signature in a header, the consumer recomputes and compares constant-time. This skill covers verification end-to-end.

      Sourced from svix-docs receiving/verifying-payloads/why and how.
  - fragment_type: rationale
    sequence: 2
    content: |
      Because of the way webhooks work, attackers can impersonate services by simply sending a fake webhook to an endpoint. Think about it: it's just an HTTP POST from an unknown source. This is a potential security hole for many applications, or at the very least, a source of problems.

      In order to prevent it, Svix signs every webhook and its metadata with a unique key for each endpoint. This signature can then be used to verify the webhook indeed comes from Svix, and only process it if it is.
  - fragment_type: rationale
    sequence: 3
    content: |
      Another potential security hole is what's called replay attacks. A [replay attack](https://en.wikipedia.org/wiki/Replay_attack) is when an attacker intercepts a valid payload (including the signature), and re-transmits it to your endpoint. This payload will pass signature validation, and will therefore be acted upon.

      To mitigate this attack, Svix includes a timestamp for when the webhook attempt occurred. Our libraries automatically reject webhooks with a timestamp that are more than five minutes away (past or future) from the current time. This requires your server's clock to be synchronised and accurate, and it's recommended that you use [NTP](https://en.wikipedia.org/wiki/Network_Time_Protocol) to achieve this.
  - fragment_type: example
    sequence: 4
    content: |
      First install the libraries if you haven't already:

      <CodeTabs items={["JavaScript","Python","Rust","Go","Java","Kotlin","Ruby","C#","PHP","CLI","cURL"]}>

      <TabItem value="JavaScript">

      ```js
      npm install svix
      // Or
      yarn add svix
      ```

      </TabItem>
      <TabItem value="Python">

      ```sh
      pip install svix
      ```

      </TabItem>
      <TabItem value="Rust">

      ```toml
      http = "1.0.0"
      svix = "1.20.0"
      ```

      </TabItem>
      <TabItem value="Go">

      ```shell
      go get github.com/svix/svix-webhooks/go
      ```

      </TabItem>
      <TabItem value="Java">

      Gradle: Add this dependency to your project's build file:

      ```groovy
      implementation "com.svix:svix:0.x.y"
      ```

      Maven: Add this dependency to your project's POM:

      ```xml
      <dependency>
        <groupId>com.svix</groupId>
        <artifactId>svix</artifactId>
        <version>0.x.y</version>
      </dependency>
      ```

      </TabItem>
      <TabItem value="Kotlin">

      Gradle: Add this dependency to your project's build file:

      ```groovy
      implementation "com.svix.kotlin:svix-kotlin:0.x.y"
      ```

      Maven: Add this dependency to your project's POM:

      ```xml
      <dependency>
        <groupId>com.svix.kotlin</groupId>
        <artifactId>svix-kotlin</artifactId>
        <version>0.x.y</version>
      </dependency>
      ```

      </TabItem>
      <TabItem value="Ruby">

      ```shell
      gem install svix
      ```

      </TabItem>
      <TabItem value="C#">

      ```shell
      dotnet add package Svix
      ```

      </TabItem>
      <TabItem value="PHP">

      ```shell
      composer require svix/svix
      ```

      </TabItem>
      <TabItem value="CLI">

      On macOS install via <a href="https://brew.sh/">Homebrew</a>:
      ```sh
      brew install svix/svix/svix-cli
      ```

      On Windows install via <a href="https://scoop.sh/">Scoop</a>:
      ```sh
      scoop bucket add svix https://github.com/svix/scoop-svix.git
      scoop install svix
      ```

      For other platforms, such as linux, checkout the <a href="https://github.com/svix/svix-cli#installation">CLI docs</a> on Github.

      </TabItem>
      <TabItem value="cURL">

      ```shell
      # Install cURL. E.g. on arch linux:
      pacman -S curl
      ```

      </TabItem>
      </CodeTabs>
  - fragment_type: execution
    sequence: 5
    content: |
      Then verify webhooks using the code below. The payload is the raw (string) body of the request, and the headers are the headers passed in the request.

      <Callout type="error">

      **Use the raw request body**

      You need to use the raw request body when verifying webhooks, as the cryptographic signature is sensitive to even the slightest changes.
      You should watch out for frameworks that parse the request as JSON and then stringify it because this too will break the signature verification.

      See examples below for how to get the raw request body with different frameworks.

      </Callout>
      The signature you should get from where you added the endpoint, e.g. the application portal.
  - fragment_type: example
    sequence: 6
    content: |
      <CodeTabs items={["JavaScript","Python","Rust","Go","Java","Kotlin","Ruby","C#","PHP","CLI","cURL"]}>

      <TabItem value="JavaScript">

      ```js

      const secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

      // These were all sent from the server
      const headers = {
        "svix-id": "msg_EXAMPLE_PLACEHOLDER",
        "svix-timestamp": "1614265330",
        "svix-signature": "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
      };
      const payload = '{"test": 2432232314}';

      const wh = new Webhook(secret);
      // Throws on error, returns the verified content on success
      wh.verify(payload, headers);
      ```

      </TabItem>
      <TabItem value="Python">

      ```python
      from svix.webhooks import Webhook

      secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET"

      # These were all sent from the server
      headers = {
        "svix-id": "msg_EXAMPLE_PLACEHOLDER",
        "svix-timestamp": "1614265330",
        "svix-signature": "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
      }
      payload = '{"test": 2432232314}'

      wh = Webhook(secret)
      # Throws on error, returns the verified content on success
      wh.verify(payload, headers)
      ```

      </TabItem>
      <TabItem value="Rust">

      ```rust
      use svix::webhooks::Webhook;

      let secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET".to_string();

      let mut headers = http::HeaderMap::new();
      headers.insert("svix-id", "msg_EXAMPLE_PLACEHOLDER");
      headers.insert("svix-timestamp", "1614265330");
      headers.insert(
          "svix-signature",
          "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
      );

      let payload = b"{\"test\": 2432232314}";

      let wh = Webhook::new(&secret)?;
      wh.verify(&payload, &headers)?;
      // returns Ok on success, Err otherwise
      ```

      </TabItem>
      <TabItem value="Go">

      ```go
      svix "github.com/svix/svix-webhooks/go"
      )

      secret := "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET"

      // These were all sent from the server
      headers := http.Header{}
      headers.Set("svix-id", "msg_EXAMPLE_PLACEHOLDER")
      headers.Set("svix-timestamp", "1614265330")
      headers.Set("svix-signature", "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=")

      payload := []byte(`{"test": 2432232314}`)

      wh, err := svix.NewWebhook(secret)
      err := wh.Verify(payload, headers)
      // returns nil on success, error otherwise
      ```

      </TabItem>
      <TabItem value="Java">

      ```java

      String secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

      // These were all sent from the server
      HashMap<String, List<String>> headerMap = new HashMap<String, List<String>>();
      headerMap.put("svix-id", Arrays.asList("msg_EXAMPLE_PLACEHOLDER"));
      headerMap.put("svix-timestamp", Arrays.asList("1614265330"));
      headerMap.put("svix-signature", Arrays.asList("v1,EXAMPLE_PLACEHOLDER_SIGNATURE="));
      HttpHeaders headers = HttpHeaders.of(headerMap, BiPredicate<String, String>)

      String payload = "{\"test\": 2432232314}";

      Webhook webhook = new Webhook(secret);

      webhook.verify(payload, headers)
      // throws WebhookVerificationError exception on failure.
      ```

      </TabItem>
      <TabItem value="Kotlin">

      ```kotlin

      val secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

      // These were all sent from the server
      val headersMap = mapOf(
          "svix-id" to listOf("msg_EXAMPLE_PLACEHOLDER"),
          "svix-timestamp" to listOf("1614265330"),
          "svix-signature" to listOf("v1,EXAMPLE_PLACEHOLDER_SIGNATURE="),
      )
      val headers = HttpHeaders(headersMap)

      val payload = "{\"test\": 2432232314}"

      val webhook = Webhook(secret)

      try {
          webhook.verify(payload, headers)
      } catch (e: WebhookVerificationError) {
          // Handle error
      }
      ```

      </TabItem>
      <TabItem value="Ruby">

      ```ruby
      require 'svix'

      secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET"

      # These were all sent from the server
      headers = {
        "svix-id" => "msg_EXAMPLE_PLACEHOLDER",
        "svix-timestamp" => "1614265330",
        "svix-signature" => "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=",
      }
      payload = '{"test": 2432232314}'

      wh = Svix::Webhook.new(secret)
      # Throws on error, returns the verified content on success
      wh.verify(payload, headers)
      ```

      </TabItem>
      <TabItem value="C#">

      ```csharp
      using Svix;

      string secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

      // These were all sent from the server
      var headers = new Dictionary<string, List<string>> {
          { "svix-id", new List<string> { "msg_EXAMPLE_PLACEHOLDER" } },
          { "svix-timestamp", new List<string> { "1614265330" } },
          { "svix-signature", new List<string> { "v1,EXAMPLE_PLACEHOLDER_SIGNATURE=" } }
      };
      string payload = "{\"test\": 2432232314}";

      var webhook = new Webhook(secret);
      // Throws on error, returns the verified content on success
      webhook.Verify(payload, headers);
      ```

      </TabItem>
      <TabItem value="PHP">

      ```php
      use Svix\Webhook;

      $secret = "whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET";

      // These were all sent from the server
      $headers = [
          'svix-id' => ['msg_EXAMPLE_PLACEHOLDER'],
          'svix-timestamp' => ['1614265330'],
          'svix-signature' => ['v1,EXAMPLE_PLACEHOLDER_SIGNATURE=']
      ];
      $payload = '{"test": 2432232314}';

      $webhook = new Webhook($secret);
      // Throws on error, returns the verified content on success
      $webhook->verify($payload, $headers);
      ```

      </TabItem>
      <TabItem value="CLI">

      ```sh
      svix verify --secret whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET --body '{"test": 2432232314}' --headers 'svix-id=msg_EXAMPLE_PLACEHOLDER' 'svix-timestamp=1614265330' 'svix-signature=v1,EXAMPLE_PLACEHOLDER_SIGNATURE='
      ```

      </TabItem>
      <TabItem value="cURL">

      ```sh
      # Install cURL. E.g. on arch linux:
      pacman -S curl
      ```

      </TabItem>
      </CodeTabs>
  - fragment_type: rationale
    sequence: 7
    content: |
      The recommended way to verify webhooks is using our official libraries as outlined in the [How to Verify](./how.mdx) section.

      However, here are instructions for verifying manually for those who need it.
  - fragment_type: example
    sequence: 8
    content: |
      Each webhook call includes three headers with additional information that are used for verification:

      * `svix-id`: the unique message identifier for the webhook message. This identifier is unique across all messages, but will be the same when the same webhook is being resent (e.g. due to a previous failure).
      * `svix-timestamp`: timestamp in [seconds since epoch](https://en.wikipedia.org/wiki/Unix_time).
      * `svix-signature`: the [Base64](https://en.wikipedia.org/wiki/Base64) encoded list of signatures (space delimited).

      <Callout type="info">

      Professional and Enterprise tier customers can have the headers white-labeled to use the `webhook-` prefix instead of the `svix-` prefix used above. The Svix libraries support both.

      </Callout>
  - fragment_type: execution
    sequence: 9
    content: |
      Constructing the signed content

      The content to sign is composed by concatenating the id, timestamp and payload, separated by the full-stop character (`.`). In code, it will look something like:

      ```javascript
      const signedContent = `${svix_id}.${svix_timestamp}.${body}`;
      ```

      Where `body` is the raw body of the request. The signature is sensitive to any changes, so even a small change in the body will cause the signature to be completely different. This means that you should *not* change the body in any way before verifying.
  - fragment_type: example
    sequence: 10
    content: |
      Determining the expected signature

      Svix uses an [HMAC](https://en.wikipedia.org/wiki/Hash-based_message_authentication_code) with [SHA-256](https://en.wikipedia.org/wiki/SHA-2) to sign its webhooks.

      So to calculate the expected signature, you should HMAC the `signed_content` from above using the base64 portion of your signing secret (this is the part after the `whsec_` prefix) as the key.
      For example, given the secret `whsec_EXAMPLE_PLACEHOLDER_NOT_A_REAL_SECRET` you will want to use `MfKQ9r8GKYqrTwjUPD8ILPZIo2LaLaSw`.

      For example, this is how you can calculate the signature in Node.js:

      ```javascript
      const crypto = require('crypto');

      const signedContent = `${svix_id}.${svix_timestamp}.${body}`;
      const secret = "whsec_5WbX5kEWLlfzsGNjH64I8lOOqUB6e8FH";

      // Need to base64 decode the secret
      const secretBytes = Buffer.from(secret.split('_')[1], "base64");
      const signature = crypto
        .createHmac('sha256', secretBytes)
        .update(signedContent)
        .digest('base64');

      console.log(signature);
      ```

      This generated signature should match one of the ones sent in the `svix-signature` header.

      The `svix-signature` header is composed of a list of space delimited signatures and their corresponding version identifiers. The signature list is most commonly of length one. Though there could be any number of signatures. For example:

      ```
      v1,EXAMPLE_PLACEHOLDER_SIGNATURE= v1,bm9ldHUjKzFob2VudXRob2VodWUzMjRvdWVvdW9ldQo= v2,MzJsNDk4MzI0K2VvdSMjMTEjQEBAQDEyMzMzMzEyMwo=
      ```

      Make sure to remove the version prefix and delimiter (e.g. `v1,`) before verifying the signature.

      Please note that to compare the signatures it's recommended to use a constant-time string comparison method in order to prevent timing attacks.
  - fragment_type: verification
    sequence: 11
    content: |
      Verify timestamp

      As mentioned above, Svix also sends the timestamp of the attempt in the `svix-timestamp` header. You should compare this timestamp against your system timestamp and ensure that it falls within an acceptable tolerance (e.g., ±5 minutes). This helps prevent replay attacks by ensuring that the webhook is not too old or too new.

      If the timestamp is outside the acceptable range, you should reject the webhook to prevent unauthorized access.